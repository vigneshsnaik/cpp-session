#include <iostream>
#include <vector>
using namespace std;
int main() {
    // for-each loop example
    vector<int> v = {0, 1, 2, 3, 4, 5};
    for (auto i : v)
        cout << i << endl;

    string sched_paramtr = "TLE-MEC";
    // print each character of the string in a new line
}